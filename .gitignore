# Operating systems
.DS_Store
Thumbs.db

# Editors
.idea/
.vscode/
*.swp
*.swo

# Local configuration and credentials
.env
.env.*
!.env.example
*.local

# Logs and runtime state
*.log
logs/
run/
tmp/

# Common dependency and build outputs
node_modules/
dist/
build/
coverage/
target/
.venv/
__pycache__/
*.py[cod]

# Command Governor substrate install roots. The tarballs under vendor/ are
# downloaded and hash-verified by scripts/bootstrap.sh; the installed tree is
# reproduced from the committed lockfile. Neither is source.
pins/*/vendor/
pins/*/node_modules/
# Vendored third-party packages: the committed tarball plus the committed patch
# under pins/patches/ are the source; scripts/bootstrap.sh extracts and patches
# them into pins/packages/<name>-<version>/ for Prime to install by path.
pins/packages/*/
