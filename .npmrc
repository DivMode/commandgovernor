# Keep a hand-run `npm ci` / `npm install` at the repository root as harmless
# as the one scripts/bootstrap.sh performs. See pins/pi-0.84.4/.npmrc for the
# dependencies that make this matter.
ignore-scripts=true
