# Two pinned transitive dependencies carry install scripts (@google/genai,
# protobufjs). scripts/bootstrap.sh passes --ignore-scripts, but that hardening
# must not depend on everyone remembering to use the script: a bare `npm ci` or
# `npm install` run in this directory by hand gets the same treatment.
ignore-scripts=true
